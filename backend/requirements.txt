# MVP runtime dependencies (face detection/landmarks/shape + beard/hair/glasses
# recommendations, served via FastAPI). No frontend deps here - the frontend
# is a separate Next.js app under /frontend.
opencv-python-headless>=4.8.0
# >=0.10.18 rather than a higher floor: Linux wheels (used in the Docker
# deploy) currently top out at 0.10.18, while Windows/py3.13 dev machines
# resolve to a newer version - pip picks the latest available per-platform,
# so this constraint needs to be satisfiable on both.
mediapipe>=0.10.18
numpy>=1.24.0
Pillow>=10.0.0
onnxruntime>=1.16.0
PyYAML>=6.0.0
pydantic>=2.0.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# "Preview on me" style visualization (POST /api/visualize) - calls Gemini's
# image-editing model. Requires a GEMINI_API_KEY (see backend/.env.example).
# The rest of the app works fine without this key; only /api/visualize
# needs it.
google-genai>=1.0.0

# Phase 2 (not needed for MVP, add when the skin/acne transfer-learning
# classifier and offline retraining pipeline are built):
# torch>=2.0.0
# torchvision>=0.15.0
# timm>=0.9.0
# albumentations>=1.3.0
# scikit-learn>=1.3.0
# pandas>=2.0.0